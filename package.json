{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:integration": "vitest run tests/integration"
  },
  "dependencies": {
    "pg": "^8.12"
  },
  "devDependencies": {
    "@types/pg": "^8.11",
    "tsx": "^4.15",
    "typescript": "^5.5",
    "vitest": "^2.0"
  }
}
